export default function Footer() {
  return (
    <footer style={{
      borderTop: '0.5px solid var(--border)',
      padding: '2rem 3rem',
      textAlign: 'center',
      fontFamily: 'var(--mono)',
      fontSize: '12px',
      color: 'var(--text-dim)',
      letterSpacing: '0.06em',
    }}>
      priya dharshini s y · built with react + vite · vellore, 2025
    </footer>
  )
}